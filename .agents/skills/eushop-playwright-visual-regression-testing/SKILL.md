---
name: eushop-playwright-visual-regression-testing
description: "Playwright Visual Regression Testing & Responsive UI Baseline Skill for EUshop"
---

# EUshop Playwright Visual Regression Testing Skill

## Overview

This skill establishes visual regression testing guidelines, screenshot baselines, and layout verification for `apps/web/`.

---

## 1. Visual Testing Rules

- Capture visual snapshot baselines for core pages: `/`, `/cart`, `/become-seller`, `/checkout`, `/impressum`.
- Verify responsive layout reflow across mobile (`375px`), tablet (`768px`), and desktop (`1440px`).
