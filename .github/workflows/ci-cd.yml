{"name": "ci-cd", "on": {"push": {"branches": ["main", "develop"]}}, "jobs": {"build-and-deploy": {"runs-on": "ubuntu-latest", "steps": [{"name": "Checkout code", "uses": "actions/checkout@v3"}, {"name": "Setup pnpm", "uses": "pnpm/action-setup@v2"}, {"name": "Install dependencies", "run": "pnpm install"}, {"name": "Build and deploy", "env": {" NODE_ENV": "production"}, "run": "pnpm run build-and-deploy"}]}}}